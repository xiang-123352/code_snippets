# -*- coding: utf-8 -*-

"""
Die Socket Klasse
Bietet nach Erzeugung weitere Methoden
Unterscheidung zwischen blocking und non-blocking mode

accept()
bind(address)
close()
connect(address)
connect_ex(address)
petpeername()
getsockname()
listen(backlog)
recv(bufsize[,flags])
recv_into(buffer[,nbytes[,flags]])
recvfrom(bufsize[,flags])
recvfrom_into(buffer[, nbytes[,flags]])
send(string[, flags])
sendall(string[, flags])
sendto(string[, flags],address)
setblocking(flag)
settimeout(value)
gettimeout()
"""
